# ENG-007 — CI verification workflow

Issue: #160
Branch: `agent/eng-007-ci-verification`

## State

IN PROGRESS.

## Checkpoint

Close the remaining CI acceptance gaps without changing runtime behavior:

1. validate the dashboard/catalog Markdown contract in CI;
2. enforce a protected release-workflow contract without running full release packaging on every PR;
3. add focused regression tests for both contracts;
4. preserve every existing Flutter CI gate and the path-triggered release APK+AAB smoke;
5. complete normal Flutter CI, Debug APK artifact evidence, and current-main reconciliation before merge.

## Acceptance

- catalog phase headings and feature-table rows remain machine-parseable by the Developer Portal contract;
- malformed/duplicate feature IDs, invalid status/priority values, and invalid dependency references fail verification;
- the dashboard HTML remains a runtime parser of `docs/FEATURE_CATALOG.md` and does not become a manually maintained status copy;
- release smoke retains input preflight, ephemeral CI signing, ads-disabled release builds, APK+AAB outputs, checksum/evidence verification, and artifact upload;
- normal Flutter CI executes both new blocking contracts before Analyze/full tests;
- focused tests, Analyze, full Flutter suite, Debug APK build, and artifact upload pass;
- branch is `behind=0` before merge.
