# ENG-005 — Clean architecture boundaries

Issue: #154
Branch: `agent/eng-005-architecture-boundaries`

## State

IN PROGRESS.

## Current checkpoint

Establish enforceable boundaries without a destabilizing repository-wide move:

1. keep `main.dart` as a thin Flutter entry point; move concrete dependency construction/lifecycle ownership into a typed composition root;
2. document dependency directions for presentation, domain/application, storage, assets, motion, analytics, and optional services;
3. add an automated source dependency contract that blocks prohibited imports from core domain/application code;
4. preserve all public screen contracts, persistence keys, ads, optional-service startup, localization, navigation, gameplay, rewards, and economy behavior.

## Acceptance

- composition root owns ProgressStore, AppSettingsStore, and OptionalServiceCoordinator creation/disposal;
- `main.dart` no longer constructs those concrete dependencies directly;
- architecture policy is executable in tests and covers prohibited direction examples;
- no save/economy/gameplay semantics change;
- format, Analyze, focused architecture/bootstrap tests, full Flutter suite, Debug APK, and artifact upload pass;
- branch is reconciled with current main before merge.
