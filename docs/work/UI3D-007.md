# UI3D-007 World Map Visual Refresh

Issue: #140
Branch: `agent/ui3d-007-world-map-refresh`

## State

IN PROGRESS until Flutter CI proves formatting, Analyze, level-select regressions, full tests, and Debug APK build.

## Scope

Continue the premium visual direction introduced by HOME-001 into the production World / Level Select screen without changing progression, economy, persistence, route identity, unlock rules, or the 150-level catalog.

## Visual direction

- Reframe the screen as a global cargo Route Network rather than a generic card grid.
- Add a dark premium global journey command header with current world, cleared cities, stars, next level, and global completion.
- Give every world a route identity with level range, completion, stars, status, and progress rail.
- Promote the current unlocked city with a NEXT marker while preserving boss-city treatment.
- Keep locked worlds visually distinct and explain the unlock dependency.
- Preserve the selected GameSkin colors and ambient reduced-motion background.

## Contracts preserved

- `World Map` / `خريطة العالم` app-bar identity.
- `Global City Journey` responsive-test identity.
- One scrollable `ListView` with per-unlocked-world `GridView` sections.
- Existing `GameNavigator.push` briefing route naming: `/briefing/level/<n>`.
- Existing guard key naming: `briefing:<n>`.
- Existing city unlock, star, boss, and 25-level-per-world logic.
- Arabic RTL and tablet large-text behavior.

## Verification

Pending pull-request CI.