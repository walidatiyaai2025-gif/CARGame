# Native 3D visual review

Install the retained QA APK after this checkpoint is promoted, then open:

`Home -> 3D -> GPU`

Expected visible result: a native GPU-rendered toy-like delivery yard with ground, roads, warehouse, delivery buildings, stylized vehicle, multiple cargo blocks, live lighting/shadows, camera orbit and draggable blue cargo. The projected scene remains one Back action away as a safe fallback.
