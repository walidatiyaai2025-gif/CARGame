name: Home Format Probe

on:
  pull_request:
    branches: [main]
    paths:
      - 'lib/features/home/home_screen.dart'
      - '.github/workflows/home_format_probe.yml'

permissions:
  contents: read

jobs:
  format-probe:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: '3.44.8'
          cache: true
      - name: Show dart format patch
        shell: bash
        run: |
          dart format lib/features/home/home_screen.dart
          echo '--- FORMAT PATCH START ---'
          git diff -- lib/features/home/home_screen.dart
          echo '--- FORMAT PATCH END ---'
