name: Apply MOT-006 checkpoint

on:
  push:
    branches: [main]
    paths:
      - scripts/apply_mot006_checkpoint.py
      - test/features/levels/world_map_ambient_background_test.dart
      - .github/workflows/apply_mot006_checkpoint.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.8
          cache: true
      - run: python scripts/apply_mot006_checkpoint.py
      - run: flutter pub get
      - run: dart format lib test
      - run: flutter analyze --no-fatal-infos --no-fatal-warnings
      - run: flutter test test/features/game/cargo_motion_tile_test.dart
      - name: Commit checkpoint
        run: |
          rm scripts/apply_mot006_checkpoint.py
          rm .github/workflows/apply_mot006_checkpoint.yml
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "feat(motion): add guarded cargo pickup and placement feedback [skip ci]"
          git push
