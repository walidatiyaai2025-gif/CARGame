name: WORLD-009 Format Probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  removed:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'Temporary WORLD-009 format probe retired.'
